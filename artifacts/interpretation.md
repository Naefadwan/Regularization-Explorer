Across the regularization path, L1 drives many coefficients exactly to zero as C decreases, with 19 features eliminated at the strongest regularization setting, while L2 keeps coefficients dense (only 0 exact zeros) and mainly shrinks magnitudes. The largest-magnitude predictors under weak regularization are cat__contract_type_Two year, cat__contract_type_Month-to-month, cat__gender_Female, suggesting they remain influential even as penalties change, whereas weaker signals are pruned first under L1 (earliest eliminations: cat__gender_Male (C=100), cat__contract_type_One year (C=100), cat__internet_service_DSL (C=100)). For this dataset, L2 is the safer default when preserving all signal is important, while L1 is preferable when feature selection and model sparsity are priorities.
